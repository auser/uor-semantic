<!-- @generated by `cargo xtask check-model --write` from model/*.toml. -->
<!-- Do not edit. The model is the single source. -->

# CONFORMANCE

Every registered ID has a Gherkin scenario and a test whose name ends in its
lowercased ID.

| Level | Meaning |
| --- | --- |
| `some-true` | Reproduced from a cited authority; not established here. |
| `build` | Constructed here and validated against a local oracle. |
| `open` | Measured and reported without a universal assertion. |

## accuracy

| ID | Level | Statement |
| --- | --- | --- |
| `AC-01` | `build` | The pinned ambiguity fixture ranks the declared contextual meaning in every registered case. |
| `AC-02` | `build` | Graph-only accuracy is evaluated on held-out contexts with exact lookup unavailable. |
| `AC-03` | `build` | Separate held-out observations are evaluated with exact-context lookup forcibly disabled. |
| `AC-04` | `build` | Graph-only parity reports the bounded indexed candidate work performed during evaluation. |
| `AC-05` | `build` | Symmetric graph cross-validation evaluates both construction directions with exact lookup disabled. |

## artifact

| ID | Level | Statement |
| --- | --- | --- |
| `AR-01` | `build` | Compiler output is accepted by the zero-copy runtime and preserves exact prediction records. |
| `AR-02` | `build` | The zero-copy runtime rejects an artifact whose stored codebook identity does not match its bytes. |
| `AR-03` | `build` | The zero-copy artifact parser rejects declared sections beyond explicit resource limits. |
| `AR-04` | `build` | Graph prediction probes a serialized bounded candidate index instead of scanning every region. |

## cli

| ID | Level | Statement |
| --- | --- | --- |
| `CL-01` | `build` | The CLI self-test executes observation parsing, compilation, validation, parity, and generation. |
| `CL-02` | `build` | Interactive artifact inspect, predict, and bounded generate commands succeed on a compiled artifact. |

## compatibility

| ID | Level | Statement |
| --- | --- | --- |
| `CX-01` | `build` | A typed R4G1 or TLA5 compatibility manifest binds source, tokenizer, and artifact identities before use. |
| `CX-02` | `build` | Compatibility status mapping preserves exact-context, graph, novel, and reserved contradictory outcomes without collapsing them. |
| `CX-03` | `build` | A compatibility witness requires the artifact identity, status, selected token, and graph region evidence appropriate to its status. |
| `CX-04` | `build` | Compatibility prediction equivalence preserves canonical token-score order, status, and widening state. |
| `CX-05` | `build` | The bounded R4G1 identity adapter maps the target header and HEAD identities into a semantic compatibility manifest. |
| `CX-06` | `build` | The bounded R4G1 identity adapter rejects truncated, unsupported, mis-sized, unaligned, and head-less containers before exposing identities. |
| `CX-07` | `build` | The borrowed R4G1 structure view exposes only canonically ordered containers containing every required graph section. |
| `CX-08` | `build` | The R4G1 structure view rejects unknown mandatory sections, missing required sections, and overlapping section bodies. |
| `CX-09` | `build` | The borrowed R4G1 graph view validates HEAD bounds and exact NODE/EDGE record lengths before exposing typed records. |
| `CX-10` | `build` | R4G1 graph validation rejects node range and depth violations, ROUT word ranges, and out-of-bounds edge endpoints. |
| `CX-11` | `build` | Typed download and compile entry points expose the R4 lifecycle surface while retaining pinned-source and validated-artifact checks. |
| `CX-12` | `build` | Typed source compilation validates the local model and corpus before invoking the Python teacher bridge, then emits a validated artifact and parity report. |
| `CX-13` | `build` | The borrowed R4G1 graph view validates ROUT v0 opcodes, termination, jumps, signature-word operands, and shortlist ranges against HEAD bounds. |
| `CX-14` | `build` | The compiler emits a deterministic structural R4G1 container with canonical section layout and valid BLAKE3 HEAD and artifact CIDs. |
| `CX-15` | `build` | The CLI compile commands write the validated `.uors` artifact and an optional structural R4G1 container through a typed output path. |
| `CX-16` | `build` | R4G1 export derives deterministic refinement edges and reverse-index ranges, while the borrowed graph view rejects invalid edge flags and reverse IDs. |
| `CX-17` | `build` | R4G1 export emits a target-framed EMIT root prior and five-level RX1 EXCT evidence, while bounded core validation rejects malformed EXCT framing. |
| `CX-18` | `build` | The no_std compatibility scorer accumulates signed R4G1 residual contributions with saturation, ignores duplicate evidence IDs, reports fixed-capacity exhaustion, and orders equal-score candidates by ascending ID. |
| `CX-19` | `build` | R4G1 export emits bounded predictive kind-2 edges from deterministic exact-context continuations, carrying accumulator-derived fixed-point scores while keeping refinement child ranges valid. |

## compiler

| ID | Level | Statement |
| --- | --- | --- |
| `CP-01` | `build` | Canonical compiler inputs and configuration produce byte-identical artifacts. |
| `CP-02` | `build` | Model build refuses unverified existing work instead of overwriting it implicitly. |

## divisible_paths

| ID | Level | Statement |
| --- | --- | --- |
| `SP-01` | `build` | Every initialized path prefix resolves as a valid divisible semantic direction. |
| `SP-02` | `build` | Compiled region paths are deterministic proximity-derived hierarchies rather than prototype-bit slots. |

## generation

| ID | Level | Statement |
| --- | --- | --- |
| `GN-01` | `build` | Greedy token generation follows a compiled exact-context teacher chain. |
| `GN-02` | `build` | Autoregressive teacher rollouts record EOS and report sequence and EOS-position parity separately. |

## huggingface

| ID | Level | Statement |
| --- | --- | --- |
| `HF-01` | `build` | Hugging Face downloads use the official CLI with an immutable revision and explicit local directory. |
| `HF-02` | `build` | Captured Hugging Face teacher observations compile into a runtime-validated semantic artifact. |
| `HF-04` | `build` | Model build validates corpus, numeric, work-directory, and bridge inputs before invoking the downloader. |
| `HF-05` | `build` | Model build passes a resolved absolute corpus path that remains valid across child working directories. |
| `HF-06` | `build` | Capture and parity bind tokenizer files, chat-template, special-token, and EOS identities to the compiled artifact. |

## model_integrity

| ID | Level | Statement |
| --- | --- | --- |
| `CM-01` | `build` | CONFORMANCE.md is generated from the checked model registers. |
| `CM-02` | `build` | Every registered capability has a Gherkin scenario and a Rust test named for its ID. |
| `CM-03` | `build` | Every cited authority has a stable citation and revision marker. |

## parity

| ID | Level | Statement |
| --- | --- | --- |
| `GP-01` | `build` | The exact-context lane reproduces captured teacher argmax on every construction fixture observation. |
| `GP-02` | `build` | Parity certification fails whenever a requested exact or graph threshold is not measured. |
| `GP-03` | `build` | Graph-only parity reports top-K recall alongside top-1 agreement. |
| `GP-04` | `build` | Parity certification can require graph coverage and overall top-K recall floors. |
| `GP-05` | `build` | The default accuracy profile uses the cross-validated bounded graph calibration settings. |
| `GP-06` | `build` | The accuracy profile preserves bounded region emissions and indexed graph work. |

## performance

| ID | Level | Statement |
| --- | --- | --- |
| `PF-01` | `build` | Reference routing remains inside a deterministic operation budget derived from declared capacities. |
| `PF-02` | `build` | The benchmark emits hardware-specific latency observations without asserting a universal wall-clock threshold. |

## runtime_contract

| ID | Level | Statement |
| --- | --- | --- |
| `RT-01` | `build` | The shipped core is dependency-free, no_std, safe, heapless, fixed-point, and free of product, quotient, and remainder operators in audited Rust source. |
| `RT-02` | `build` | A warmed reference routing call performs zero observed heap allocations and deallocations. |
| `RT-03` | `build` | Artifact parsing, prediction, and greedy generation perform zero observed heap operations after setup. |

## semantic_routing

| ID | Level | Statement |
| --- | --- | --- |
| `SR-01` | `build` | One context may retain multiple calibrated semantic memberships. |
| `SR-02` | `build` | Candidate input order does not change canonical route-cloud output. |
| `SR-03` | `build` | Compiler calibration retains an observation in every prototype region within the deterministic overlap margin. |
| `RH-01` | `build` | Masked Hamming membership includes observations exactly on the calibrated radius. |
| `RC-01` | `build` | A full active cloud reports deterministic bounded truncation instead of growing. |

## Cited authorities

| Authority | Citation | Local evidence |
| --- | --- | --- |
| `AUTH-TEMPLATE` | https://github.com/UOR-Foundation/template at commit 0a1c799338d7db829aa23365e1acf4f9d01ff8b5 | `CM-01`, `CM-02`, `CM-03` |
| `AUTH-RUST-PRACTICE` | https://gist.github.com/auser/c3161f55a8393faa8af5ddda68c6befa at revision ac2e9cedc9f69d69398de98a0ab38d47c5b5baec | `RT-01`, `RT-02`, `PF-01`, `PF-02` |
| `AUTH-HF-CLI` | https://huggingface.co/docs/huggingface_hub/en/guides/cli | `HF-01` |

## Claims that are not conformance IDs

| ID | Level | Claim |
| --- | --- | --- |
